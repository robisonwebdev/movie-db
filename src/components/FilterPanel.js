import React from 'react';
import Sort from './Sort';

const FilterPanel = () => {
    return (
        <div className='filterPanel'>
            <Sort />
        </div>
    )
};

export default FilterPanel;
